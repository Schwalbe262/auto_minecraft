# 임시 단축바 중지 재발 방지

## 확인한 현장 상태

2026-09-11 09:58 KST 진단에서 결정복제기 작업의 임시 보관 기록은 다음과 같았다.

- 원본: `society:fire_quartz` 2개, 단축바 인덱스 5 → 일반 인벤토리 인덱스 9.
- 단계: PARKED. 중지 이유: 원래 단축바 아이템의 정확한 위치 확인 불가.
- 사용자는 이후 직접 화염석을 보관함에 정리했다고 확인했다. 현재 아이템을 되돌려 놓거나 기록만 지우는 해결을 요청하지 않았다.
- 실행 중인 JAR은 `82CE26255B6B9A4BC8C0D290E24C3E30FC6474F62DF6678473F0E14B9B2EB678`로, 앞선 결정 일반화 변경은 아직 로드되지 않은 상태였다.

원본을 임시로 옮긴 뒤 회수한 같은 결정이 합쳐지면 원본 수량·fingerprint가 달라진다. 당시 최초 불일치 순간의 전체 패킷은 이 진단만으로 확정할 수 없지만, 결정을 임대 원본으로 고르는 위험한 경로가 실제 실행된 것은 확인했다.

## 재발 방지

1. 앞선 `eda5874`의 제외 규칙을 유지한다. 복제 가능한 기본 56종과 pristine 56종 모두 결정 작업의 임대 원본이 될 수 없다. 옥만 제외하는 방식이 아니다.
2. 임대 원본은 그대로인데 수거물이 빈 작업 칸에 들어오는 경우를 처리한다. 그 결정만 일반 인벤토리 빈칸으로 옮기고 실제 SWAP 응답을 기다린다.
3. 최신 실제 전체 인벤토리 패킷 이후에 온 **개별 슬롯 패킷**을 현재 두 칸의 상태 확인에 반영한다. 예전 전체 패킷의 빈칸과 최신 수거물이 들어온 칸을 비교해 복원 실패로 오판하지 않는다.

## 유지하는 안전 조건

- 현재 연결의 가장 최신 menu 0 FULL 46칸을 기준으로 한다. 이전 FULL을 되살리지 않는다.
- 이후 같은 menu 0의 실제 packetItem 중 각 칸의 최신 sequence만 반영한다. 클라이언트가 예측한 메뉴 상태는 증거로 쓰지 않는다.
- FULL의 실제 sequence를 바꾸지 않는다. 개별 슬롯 패킷으로 이전 수동 조작·미완료 교환 경계를 넘기지 않는다.
- 원본 수량·NBT·fingerprint가 실제로 달라진 경우는 성공으로 간주하지 않는다. 원본의 양 끝 중복, 빈 커서 미확인, 미완료·늦게 도착한 조작 보호도 유지한다.
- 이 변경은 현재 보관 상태의 판정일 뿐, 실패한 과거 클릭을 성공 처리하거나 자동 재클릭하는 코드가 아니다.
- 사용자가 직접 정리한 현재 인벤토리와 임시 보관 기록은 이 작업에서 변경하지 않는다.

## 반복 검증

단일 성공 확인뿐 아니라 가득 찬 단축바, 화염석 및 모든 지원 결정, 여러 기기, 연속 수거·작업 칸 비우기·원본 복원을 반복한다. 슬롯 상태 변화와 조작 ACK를 분리하여 상태가 먼저 바뀌어도 확인되지 않은 작업이 완료되지 않는지 검증한다.

- 전체 `gradle --offline test build`: 2,476개 테스트 통과, 실패·오류 0개.
- 동일 모듈 32회 작업, 기기 96회 수거, 단축바에 112종 전체 노출. 매회 전량 보관·비결정 원본 복원·임시 보관 기록 정상 종료를 확인했다.
- 각 회차의 PARK/CLEAR/RESTORE에서 현재 보관 상태가 먼저 확인돼도 해당 조작 ACK가 오기 전에는 다음 동작으로 진행하지 않는 것을 확인했다.
- 설치 호환성 검사 통과. 빌드 JAR SHA256: `F2613AEAD9B93930CA5990199D658C108B8E60527737CF247A9B3E586A5FED65`.
- 위 반복 횟수는 자동화된 회귀 테스트다. 실행 중인 게임에서 32일을 진행했거나 장시간 무중단 운전을 확인했다는 뜻은 아니다. 기존 게임은 재시작해야 새 코드가 적용된다.

## 실제 설치·재시작 확인

사용자의 재시작 승인 후 2026-09-11 10:17 KST에 기존 Society 클라이언트를 정상 종료했다. 종료가 확인된 뒤 수정 JAR을 설치하고 같은 인스턴스를 다시 실행했다. 10:19 KST의 새 진단에서 서버 접속 완료(`connected=true`), 게임 화면(`screenClass=null`), 자동화 OFF를 확인했다. 설치된 JAR의 SHA256은 위 검증 빌드와 일치한다.

기존 모드 파일과 프로필은 인스턴스의 `local/autovalley-backups`에 백업했다. 아이템 이동이나 임시 보관 기록 삭제는 실행하지 않았고, 원본 임시 보관 기록 및 밭 3개 등록이 보존된 것을 확인했다. 따라서 이번 재시작은 이전 수동 정리의 복원 기록을 해제하거나 자동화를 재개한 작업은 아니다.

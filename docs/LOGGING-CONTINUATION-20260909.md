# Logging continuation checkpoint — 2026-09-09

The leaf-stance retry, tomato stock cache and TrashSlot-buffer changes were
installed together after a normal shutdown/restart. The first native warehouse
survey published all 32 ordinary containers with no dirty entries and the default
three-day interval. One shipment confirmed 27 inventory transfers totaling 1,600
tomatoes, then continued to seed-making and crystal replication without another
warehouse survey in that shipment. This proves initial cache publication and that
shipment, not a later cached machine refill or multi-day expiry behavior.

A subsequent logging batch completed 11 confirmed strokes, then its next stroke
failed the combined distance/posture/tool boundary. The retained attempt had sent
START but not STOP. One-second observations show repositioning between strokes;
they do not identify the precise failed predicate. A later read found the original
axe still in its registered slot with unchanged remaining durability, but the
operator had already moved and selected another item. Current geometry therefore
cannot be substituted for failure-time evidence.

The follow-up addresses two independent continuation hazards:

- Stop residual travel and observe a short, actual grounded quiet-pose window
  before starting mining. Continuing stationary strokes need no repeated fixed
  sleep. New movement, target changes and discontinuous tick observations revoke
  that readiness; current visibility is still required before dispatch.
- A previous confirmed stroke's delayed positive axe-wear update must not look
  like a tool replacement merely because it arrives after the next START. Only
  unchanged metadata or increasing Damage is compatible. Repair/decreasing
  Damage, different items and all other native tag changes remain rejected.

Continuation failures now name authority, grounding, crouching, reach, selected
slot, registered axe or native metadata separately. None of these changes clears
an uncertain reply, fabricates completion, bypasses a pre-STOP ray, or resends
START/STOP. The retained failed run is preserved for normal reconnect/resume.
Long-duration stability is not established by this checkpoint.

## Validation and deployment status

The combined offline `test build` passed 1,987 tests across 147 suites, with no
failures, errors or skipped tests. The follow-up artifact SHA-256 is
`D7B04D5AB2404BC8A1526C6BE197D9694E654157C73B0A9F6A664FFF0664615F`.
An independent read-only review found no blocker in the quiet-pose dispatch gate.

The installed client already contains the stock-cache, leaf-stance retry and
unrestricted retained TrashSlot-buffer changes. The continuation artifact above
has **not** yet been installed or validated in-game. At the final preflight the
operator was playing in a different dimension/profile from the registered farm;
the exact-profile inspection refused to proceed. No profile was forced, no
teleport or automation start was issued, and no retained failure was erased.
Resume native validation after the operator returns to the registered farm.
